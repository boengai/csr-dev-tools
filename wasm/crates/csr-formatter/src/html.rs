pub fn format(input: &str, _indent: u32, _use_tabs: bool) -> String {
    if input.trim().is_empty() {
        return String::new();
    }
    String::new()
}

pub fn minify(input: &str) -> String {
    if input.trim().is_empty() {
        return String::new();
    }
    String::new()
}
